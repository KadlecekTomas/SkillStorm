node_modules/
dist/
.env
.env.local
.env.production
.env.test
.env.development
.env.staging
.env.*.local
.DS_Store

# Prevent stale compiled artifacts in src/ from shadowing .ts (causes "X is not a function")
server/src/shared/cache/*.js
server/src/shared/cache/*.d.ts
server/src/shared/cache/*.js.map
client/.env
server/.env
audit-report.json
*.log
logs/
client/tests/test-results/
client/test-results/
test-results/
client/tests/playwright-report/
client/tests/snapshots/
.playwright-mcp/

# Database backups (scripts/ops/backup-db.sh) — never commit dumps
backups/
docs/screenshots/portfolio/.frame-*.html

# Zastaralý build artefakt (server/.dockerignore ho ignoruje taky)
disttmp/

# Playwright-generated authenticated browser state — obsahuje živé session
# cookies (access/refresh token). Regeneruje se v auth.setup.ts při každém běhu.
client/tests/scenarios/.auth/
