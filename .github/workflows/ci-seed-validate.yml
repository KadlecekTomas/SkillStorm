name: SkillStorm CI – Seed & Validate

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  seed-and-validate:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        ports:
          - "5432:5432"
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: skillstorm
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/skillstorm

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: |
            client/package-lock.json
            server/package-lock.json

      - name: 📦 Install dependencies
        run: npm ci
        working-directory: server

      - name: 🛠️ Generate Prisma client
        run: npx prisma generate
        working-directory: server

      - name: 🧱 Run database migrations
        run: npx prisma migrate deploy
        working-directory: server

      - name: 🌱 Seed the database
        run: npx ts-node prisma/seed/e2e-seed.ts
        working-directory: server

      - name: 🔍 Validate seed data
        run: npx ts-node prisma/seed/validate-seed.ts
        working-directory: server

      - name: ✅ Done
        if: success()
        run: echo "All seed data validated successfully 🎉"
