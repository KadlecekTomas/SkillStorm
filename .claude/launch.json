{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "client",
      "runtimeExecutable": "env",
      "runtimeArgs": ["API_PROXY_TARGET=http://localhost:4201", "npm", "run", "dev", "--prefix", "client"],
      "port": 3000
    }
  ]
}
